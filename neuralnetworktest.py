# Neural networks are best for vision
# Most time used for neural network is used for gaining data
# Use out-of-sample data to train neural network as it has never seen that data before.
# Do not use in-sample data to train neural network.

import torch
import torchvision
from 
